What does the command uname print when you run it without any option?
The command "uname" as the name implies which is 'print system information' so therefore the name uname prints out
print the kernel name. 
